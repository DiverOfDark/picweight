// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type ClientMeta, formDataBodySerializer, type Options as Options2, type RequestResult, type ServerSentEventsResult, type TDataShape } from './client';
import { client } from './client.gen';
import type { AuthConfigData, AuthConfigResponses, CallbackData, CallbackErrors, ClientVersionData, ClientVersionResponses, CreateMealData, CreateMealErrors, CreateMealResponses, DeleteMealData, DeleteMealErrors, DeleteMealResponses, ExportDataData, ExportDataErrors, ExportDataResponses, GetDayData, GetDayErrors, GetDayResponses, GetGroupData, GetGroupErrors, GetGroupResponses, GetMealData, GetMealErrors, GetMealResponses, GetMealThumbnailData, GetMealThumbnailErrors, GetMealThumbnailResponses, GetMeData, GetMeErrors, GetMeResponses, HealthzData, HealthzResponses, ListMealsData, ListMealsResponses, ListWeightsData, ListWeightsResponses, LoginData, LogoutData, LogWeightData, LogWeightErrors, LogWeightResponses, MealEventsData, MealEventsErrors, MealEventsResponse, MealEventsResponses, MealRevisionsData, MealRevisionsErrors, MealRevisionsResponses, PatchMealData, PatchMealErrors, PatchMealResponses, ReanalyzeMealData, ReanalyzeMealErrors, ReanalyzeMealResponses, RecentDishesData, RecentDishesResponses, RefreshData, RefreshErrors, RefreshResponses, ResolveBarcodeData, ResolveBarcodeErrors, ResolveBarcodeResponses, RetryMealData, RetryMealErrors, RetryMealResponses, SessionInfoData, SessionInfoErrors, SessionInfoResponses, TokenExchangeData, TokenExchangeErrors, TokenExchangeResponses, UpdateProfileData, UpdateProfileErrors, UpdateProfileResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Handle the OIDC redirect
 *
 * Verifies the CSRF token against the state cookie, exchanges the authorization code with the PKCE verifier, validates the ID token (issuer, audience, nonce, signature), upserts the `users` row and sets the `picweight_session` cookie.
 */
export const callback = <ThrowOnError extends boolean = false>(options: Options<CallbackData, ThrowOnError>): RequestResult<unknown, CallbackErrors, ThrowOnError> => (options.client ?? client).get<unknown, CallbackErrors, ThrowOnError>({ url: '/api/auth/callback', ...options });

/**
 * OIDC configuration
 *
 * Issuer, client ids and scopes, so the Android app and the SPA only need to be told the picweight server URL.
 */
export const authConfig = <ThrowOnError extends boolean = false>(options?: Options<AuthConfigData, ThrowOnError>): RequestResult<AuthConfigResponses, unknown, ThrowOnError> => (options?.client ?? client).get<AuthConfigResponses, unknown, ThrowOnError>({ url: '/api/auth/config', ...options });

/**
 * Initiate OIDC login
 *
 * Starts the authorization-code + PKCE flow for the confidential web client. The PKCE verifier, nonce and CSRF token are stored in a short-lived signed cookie so the callback needs no server-side session store.
 */
export const login = <ThrowOnError extends boolean = false>(options?: Options<LoginData, ThrowOnError>): RequestResult<unknown, unknown, ThrowOnError> => (options?.client ?? client).get<unknown, unknown, ThrowOnError>({ url: '/api/auth/login', ...options });

/**
 * Log out
 *
 * Clears the `picweight_session` cookie and redirects to the login page. Bearer-token clients simply discard their token; nothing is stored server-side.
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>): RequestResult<unknown, unknown, ThrowOnError> => (options?.client ?? client).get<unknown, unknown, ThrowOnError>({ url: '/api/auth/logout', ...options });

/**
 * Current session claims
 *
 * Returns the claims of the presented session JWT without touching the database or the IdP. Use `GET /api/v1/me` for the profile and today's targets.
 */
export const sessionInfo = <ThrowOnError extends boolean = false>(options?: Options<SessionInfoData, ThrowOnError>): RequestResult<SessionInfoResponses, SessionInfoErrors, ThrowOnError> => (options?.client ?? client).get<SessionInfoResponses, SessionInfoErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/auth/me',
    ...options
});

/**
 * Refresh the session JWT
 *
 * Accepts a currently-valid session (bearer header or cookie) and issues a new JWT with a fresh expiry. An expired session cannot be resurrected — that path is a full re-login. Cookie callers also get the cookie re-set.
 */
export const refresh = <ThrowOnError extends boolean = false>(options?: Options<RefreshData, ThrowOnError>): RequestResult<RefreshResponses, RefreshErrors, ThrowOnError> => (options?.client ?? client).post<RefreshResponses, RefreshErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/auth/refresh',
    ...options
});

/**
 * Exchange an OIDC ID token for a session JWT
 *
 * The Android app runs authorization-code + PKCE against the **native (public)** client itself, then posts the resulting ID token here. The token is verified against the provider's JWKS — accepted for either the web or the mobile client id — and a picweight session JWT is returned for use as `Authorization: Bearer`.
 */
export const tokenExchange = <ThrowOnError extends boolean = false>(options: Options<TokenExchangeData, ThrowOnError>): RequestResult<TokenExchangeResponses, TokenExchangeErrors, ThrowOnError> => (options.client ?? client).post<TokenExchangeResponses, TokenExchangeErrors, ThrowOnError>({
    url: '/api/auth/token',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Resolve a barcode to a food record
 *
 * Reads the local `foods` cache first, then Open Food Facts. `POST` rather than `GET` because a miss performs an outbound fetch and writes the cache.
 */
export const resolveBarcode = <ThrowOnError extends boolean = false>(options: Options<ResolveBarcodeData, ThrowOnError>): RequestResult<ResolveBarcodeResponses, ResolveBarcodeErrors, ThrowOnError> => (options.client ?? client).post<ResolveBarcodeResponses, ResolveBarcodeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/barcode/{ean}',
    ...options
});

/**
 * Bundled Android client version
 *
 * Metadata for the APK this server ships, so the Android app can offer an in-app update. Unauthenticated, like the /picweight.apk download it describes. Always 200: a server with no bundled APK answers `available: false` with `version_code: 0`, which the client reads as "up to date".
 */
export const clientVersion = <ThrowOnError extends boolean = false>(options?: Options<ClientVersionData, ThrowOnError>): RequestResult<ClientVersionResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ClientVersionResponses, unknown, ThrowOnError>({ url: '/api/v1/client/version', ...options });

/**
 * Get a day's totals and remaining budget
 */
export const getDay = <ThrowOnError extends boolean = false>(options: Options<GetDayData, ThrowOnError>): RequestResult<GetDayResponses, GetDayErrors, ThrowOnError> => (options.client ?? client).get<GetDayResponses, GetDayErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/days/{date}',
    ...options
});

/**
 * Recent confirmed dishes
 *
 * Powers the capture screen's recent-dish chips — one tap sets the dish name, no keyboard. Confirmed meals only.
 */
export const recentDishes = <ThrowOnError extends boolean = false>(options?: Options<RecentDishesData, ThrowOnError>): RequestResult<RecentDishesResponses, unknown, ThrowOnError> => (options?.client ?? client).get<RecentDishesResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/dishes/recent',
    ...options
});

/**
 * Export all data
 */
export const exportData = <ThrowOnError extends boolean = false>(options?: Options<ExportDataData, ThrowOnError>): RequestResult<ExportDataResponses, ExportDataErrors, ThrowOnError> => (options?.client ?? client).get<ExportDataResponses, ExportDataErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/export',
    ...options
});

/**
 * Get a sitting
 *
 * Member meals plus combined totals. Each meal was analysed by its own independent loop and stays independently correctable.
 */
export const getGroup = <ThrowOnError extends boolean = false>(options: Options<GetGroupData, ThrowOnError>): RequestResult<GetGroupResponses, GetGroupErrors, ThrowOnError> => (options.client ?? client).get<GetGroupResponses, GetGroupErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/groups/{group_id}',
    ...options
});

/**
 * Current user, profile and today's state
 */
export const getMe = <ThrowOnError extends boolean = false>(options?: Options<GetMeData, ThrowOnError>): RequestResult<GetMeResponses, GetMeErrors, ThrowOnError> => (options?.client ?? client).get<GetMeResponses, GetMeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me',
    ...options
});

/**
 * Update body data and recompute targets
 *
 * Targets are computed with Mifflin-St Jeor — deterministic, offline and auditable. A deficit steeper than ~1% bodyweight/week is returned as a warning, not an error.
 */
export const updateProfile = <ThrowOnError extends boolean = false>(options: Options<UpdateProfileData, ThrowOnError>): RequestResult<UpdateProfileResponses, UpdateProfileErrors, ThrowOnError> => (options.client ?? client).put<UpdateProfileResponses, UpdateProfileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/profile',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List meals
 */
export const listMeals = <ThrowOnError extends boolean = false>(options?: Options<ListMealsData, ThrowOnError>): RequestResult<ListMealsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListMealsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals',
    ...options
});

/**
 * Log a meal
 *
 * Multipart ingest. Idempotent on `client_uuid`: replaying the same key returns the existing meal with `deduplicated: true` rather than creating a duplicate. Returns 202 — analysis is always asynchronous.
 */
export const createMeal = <ThrowOnError extends boolean = false>(options: Options<CreateMealData, ThrowOnError>): RequestResult<CreateMealResponses, CreateMealErrors, ThrowOnError> => (options.client ?? client).post<CreateMealResponses, CreateMealErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * Stream meal analysis events
 *
 * Server-sent events for this user only. Each `completed`, `reanalyzed` and `group_settled` event carries the full day state so a client can render the notification without a follow-up request.
 */
export const mealEvents = <ThrowOnError extends boolean = false>(options?: Options<MealEventsData, ThrowOnError, MealEventsResponse>): Promise<ServerSentEventsResult<MealEventsResponses>> => (options?.client ?? client).sse.get<MealEventsResponses, MealEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals/events',
    ...options
});

/**
 * Delete a meal
 */
export const deleteMeal = <ThrowOnError extends boolean = false>(options: Options<DeleteMealData, ThrowOnError>): RequestResult<DeleteMealResponses, DeleteMealErrors, ThrowOnError> => (options.client ?? client).delete<DeleteMealResponses, DeleteMealErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals/{id}',
    ...options
});

/**
 * Get a meal
 */
export const getMeal = <ThrowOnError extends boolean = false>(options: Options<GetMealData, ThrowOnError>): RequestResult<GetMealResponses, GetMealErrors, ThrowOnError> => (options.client ?? client).get<GetMealResponses, GetMealErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals/{id}',
    ...options
});

/**
 * Confirm or edit a meal
 *
 * Confirming a meal is what feeds `recall_similar_meals` — only confirmed meals, latest revision, are ever recalled. Every changed field is recorded in `item_corrections`.
 */
export const patchMeal = <ThrowOnError extends boolean = false>(options: Options<PatchMealData, ThrowOnError>): RequestResult<PatchMealResponses, PatchMealErrors, ThrowOnError> => (options.client ?? client).patch<PatchMealResponses, PatchMealErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Correct a meal by conversation
 *
 * Loads the persisted rig thread, appends the feedback as a new user turn and continues the same conversation, emitting a new revision. Prior revisions are retained. Returns 202 — the correction runs in the background.
 */
export const reanalyzeMeal = <ThrowOnError extends boolean = false>(options: Options<ReanalyzeMealData, ThrowOnError>): RequestResult<ReanalyzeMealResponses, ReanalyzeMealErrors, ThrowOnError> => (options.client ?? client).post<ReanalyzeMealResponses, ReanalyzeMealErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals/{id}/reanalyze',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Retry a failed analysis
 *
 * Re-runs the estimation agent for a meal whose analysis failed — a quota-exhausted key, a rate limit, a provider hiccup. Nothing is needed from the client: the 768px thumbnail is already stored server-side, so the retry re-reads the same image. The job is enqueued at the **same revision** — this is another attempt at the same estimate, not a correction — and the failure reason stops being reported the moment the meal leaves `failed`. Returns 202; the result arrives over SSE or by polling, as for any analysis.
 */
export const retryMeal = <ThrowOnError extends boolean = false>(options: Options<RetryMealData, ThrowOnError>): RequestResult<RetryMealResponses, RetryMealErrors, ThrowOnError> => (options.client ?? client).post<RetryMealResponses, RetryMealErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals/{id}/retry',
    ...options
});

/**
 * Revision history
 */
export const mealRevisions = <ThrowOnError extends boolean = false>(options: Options<MealRevisionsData, ThrowOnError>): RequestResult<MealRevisionsResponses, MealRevisionsErrors, ThrowOnError> => (options.client ?? client).get<MealRevisionsResponses, MealRevisionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals/{id}/revisions',
    ...options
});

/**
 * Get a meal's thumbnail
 */
export const getMealThumbnail = <ThrowOnError extends boolean = false>(options: Options<GetMealThumbnailData, ThrowOnError>): RequestResult<GetMealThumbnailResponses, GetMealThumbnailErrors, ThrowOnError> => (options.client ?? client).get<GetMealThumbnailResponses, GetMealThumbnailErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/meals/{id}/thumbnail',
    ...options
});

/**
 * Weight history
 */
export const listWeights = <ThrowOnError extends boolean = false>(options?: Options<ListWeightsData, ThrowOnError>): RequestResult<ListWeightsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListWeightsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/weights',
    ...options
});

/**
 * Log a weight
 *
 * Recomputes the user's targets from the new weight, since targets are derived from body data and must track it.
 */
export const logWeight = <ThrowOnError extends boolean = false>(options: Options<LogWeightData, ThrowOnError>): RequestResult<LogWeightResponses, LogWeightErrors, ThrowOnError> => (options.client ?? client).post<LogWeightResponses, LogWeightErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/weights',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Health check
 *
 * Deliberately does *not* touch the database: a probe that fails because
 * SQLite is briefly locked would restart a pod that is merely busy.
 */
export const healthz = <ThrowOnError extends boolean = false>(options?: Options<HealthzData, ThrowOnError>): RequestResult<HealthzResponses, unknown, ThrowOnError> => (options?.client ?? client).get<HealthzResponses, unknown, ThrowOnError>({ url: '/healthz', ...options });
